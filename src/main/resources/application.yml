
spring:
  application:
    name: pair4Project
  datasource:
    url: 'jdbc:postgresql://localhost:5432/CRM'
    username: 'postgres'
    password: 'test'
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true
    hibernate:
      ddl-auto: update
