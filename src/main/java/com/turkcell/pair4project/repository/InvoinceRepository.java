package com.turkcell.pair4project.repository;

import com.turkcell.pair4project.entity.Invoince;
import org.springframework.data.jpa.repository.JpaRepository;

public interface InvoinceRepository extends JpaRepository<Invoince, Integer> {
}
